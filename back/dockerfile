# ベースイメージを指定
FROM python:3.9

# 作業ディレクトリを設定
WORKDIR /app

# 必要なファイルをコピー
COPY . /app

# Flaskをインストール
RUN pip install Flask

# 必要な依存関係をインストール
RUN pip install <必要なライブラリ>

# ポートを公開
EXPOSE 5000

# コンテナ内で実行するコマンドを指定
CMD ["python", "app.py"]
